---
description: List tasks from the todo list
user-invocable: true
argument: optional flags (--all or -a to show completed tasks)
---

# List Tasks

List tasks from the todo list.

## Instructions

Run the following command to list tasks:

```bash
uv run todo list
```

If the user wants to see all tasks including completed ones, use:

```bash
uv run todo list --all
```

Display the output to the user in a readable format.
